Нужно, чтобы бот в фоне отслеживал монеты из /auto и автоматически открывал и закрывал SHORT, в зависимости от спреда между ценой на MEXC futures и ценой на DEX (DexScreener).


1. На каких монетах работать
 • Используем только монеты из списка /auto.
 • Для каждой монеты в /auto должны быть поля:
 • mexcSymbol – тикер фьючерсного контракта на MEXC (как в /lb /sb /lm /sm).
 • dexPair / dexPairId – идентификатор пары в DexScreener (по нему берём цену с DEX).
 • Если для монеты нет dexPair – в этом модуле её просто пропускаем.

⸻

2. Откуда брать цены
Для каждой монеты из /auto:
 1. Цена MEXC (futures)
Берём так же, как сейчас используешь для ордеров:
 • последняя цена / best bid / best ask – по уже существующей логике.
Обозначим: mexcPrice.
 2. Цена DEX (DexScreener)
Через DexScreener API по dexPair получаем актуальную цену.
Обозначим: dexPrice.

 3. Расчёт спреда
spreadPercent = |mexcPrice - dexPrice| / dexPrice * 100

3. Условие входа в авто-SHORT
Если по монете выполняется:

spreadPercent >= 13%
и mexcPrice > dexPrice
и НЕТ открытой позиции / активного ордера на SHORT по этой монете

→ модуль один раз автоматически открывает SHORT, как если бы я сам вызвал:
/sm SYMBOL

Нужно просто дернуть тот же handler, который уже используется для /sm.

4. Ограничение по количеству сделок
 • По одной монете нельзя открывать несколько авто-SHORT одновременно.
 • Если уже есть:
 • открытая SHORT/ LONG позиция по этой монете,
 • или висит лимитный/рыночный ордер на вход, созданный модулем,
→ новый вход для этой монеты не делать, пока предыдущая позиция не будет закрыта.

5. Режим работы (фон)
 • Модуль работает автоматически в фоне (отдельный цикл/джоб).
 • Период опроса: разумный интервал (например 15 секунд), чтобы не уткнуться в лимиты MEXC и DexScreener.
 • На каждой итерации:
 1. Берём список монет /auto.
 2. Для каждой монеты считаем mexcPrice, dexPrice, spreadPercent.
 3. Применяем правила входа/выхода (см. ниже).

Желательно сделать возможность включать/выключать модуль, например командами /spread_on / /spread_off.

6. Авто-закрытие SHORT при маленьком спреде
Если по монете есть открытая SHORT-позиция (от этого модуля или вообще любая SHORT по SYMBOL) и:
spreadPercent < 2%

→ модуль должен автоматически закрыть SHORT через существующую команду:
/closebs SYMBOL

Важно:
 • Не менять логику самой команды /closebs — я хочу по-прежнему иметь возможность вызывать её вручную.
 • В модуле просто нужно программно вызвать тот же handler, который обрабатывает /closebs, с тем же SYMBOL.


7. Гистерезис для повторных входов (зона 7–8%)
Чтобы модуль не “дёргался” и не открывал шорты слишком часто при небольших колебаниях спреда, вводим гистерезис:
 • Вход в SHORT возможен только при spreadPercent >= 13% (как описано выше).
 • После того как по монете уже был спред ≥13% (и модуль это отработал),
пока спред не опустится ниже 7%, новые авто-SHORT по этой монете не открывать, даже если позиция уже закрыта.
 • Как только spreadPercent < 7% — считаем, что “цикл” завершён, и при следующем росте спреда до ≥13% можно снова открывать новый SHORT.

(Порог можешь вынести в константы: ENTRY_SPREAD = 13, RESET_SPREAD = 7.)

8. Лог в Telegram
Когда модуль автоматически открыл SHORT, отправлять сообщение:

🔻 Авто-SHORT по спреду
Монета: SYMBOL
Цена MEXC: X.XXXXX
Цена DEX: Y.YYYYY
Спред: Z.ZZ%

Когда модуль автоматически закрыл SHORT по условию < 2%:

✅ Авто-закрытие SHORT по спреду
Монета: SYMBOL
Текущий спред: Z.ZZ% (< 2%)
Вызвана команда /closebs SYMBOL